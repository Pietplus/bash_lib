#!/bin/bash

# podman aliase
alias pcsv="podman-compose save"
alias pcld="podman-compose load"
alias pcb="podman-compose build"
alias pcr="podman-compose run"
alias pce="podman-compose exec"
alias pcc="podman-compose config"
alias pccv="podman-compose convert"
alias pclogs="podman-compose logs"
alias pcp="podman-compose pause"
alias pcup="podman-compose unpause"
alias pcre="podman-compose restart"

# podman-machine aliases
alias pmm="podman-machine"
alias pmc="podman-machine create"
alias pme="podman-machine env"
alias pms="podman-machine start"
alias pmst="podman-machine stop"
alias pmr="podman-machine restart"
alias pmre="podman-machine restart"
alias pmstat="podman-machine status"
s
